# MCP Transport Configuration
# Options: stdio | httpStream
# Default: stdio
MCP_TRANSPORT=stdio

# HTTP Stream Port Configuration
# Only used when MCP_TRANSPORT=httpStream
# Default: 3000
MCP_PORT=3000

